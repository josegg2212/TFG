# 🤖 NLU Tool

Esta herramienta permite introducir por consola inputs de texto, y generar dinámicamente salidas (diccionarios) que posteriormente podría interpretar una aplicación de diálogo.

## 🚀 Instalación

Instala las dependencias necesarias
    - pip install -r requirements.txt


## ▶️ Uso

Ejecuta el archivo main.py desde la consola con los siguientes argumentos:

    - python main.py -d <domain> -c <config_file> -l <logging_level>

-d, --domain → Dominio a utilizar. Opciones válidas: 'gsc' y 'keenon'.
-c, --config_file → Ruta al archivo de configuración. Por defecto se encuentra en ./config/config.json
-l, --logging_level → Opcional. Nivel para los logs. Por defecto está en INFO, y puedes seleccionar 'DEBUG' y 'ERROR'